#include<stdio.h>
int main()
{
    int a,b,c;
    int add;
    printf("Enter the the two numbers :");
    scanf("%d%d",&a,&b);
    add=a+b;
    printf("Addition of two numberd :%d",add);
    
    return 0;
}